<Window x:Class="MeloncherWPF.Views.Windows.AccountsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MeloncherWPF.ViewModels"
        xmlns:local="clr-namespace:MeloncherWPF.Views.Windows"
        mc:Ignorable="d"
        Title="AccountsWindow" Height="450" Width="800">

    <Window.DataContext>
        <vm:AccountsViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="64"></RowDefinition>
        </Grid.RowDefinitions>
        <ListView ItemsSource="{Binding Accounts}" ItemContainerStyle="{StaticResource Account}"></ListView>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Content="Add Offline" Height="32" Width="128" />
            <Button Content="Add Mojang" Height="32" Width="128" />
            <Button Content="Add Microsoft" Height="32" Width="128" />
            <Button Content="Remove Selected" Height="32" Width="128" />
        </StackPanel>
    </Grid>
</Window>
