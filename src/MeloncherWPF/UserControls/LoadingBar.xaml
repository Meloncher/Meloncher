<UserControl x:Class="MeloncherWPF.UserControls.LoadingBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="20" d:DesignWidth="512"
             x:Name="Root">
	<Grid>
		<ProgressBar Value="{Binding ElementName=Root, Path=Value}" Background="Black" Foreground="Red" BorderThickness="0" />
		<TextBlock Text="{Binding ElementName=Root, Path=Text}" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" />
		<Grid.Style>
			<Style TargetType="Grid">
				<Style.Triggers>
					<DataTrigger Binding="{Binding ElementName=Root, Path=Hidden}" Value="True">
						<Setter Property="Visibility" Value="Collapsed" />
					</DataTrigger>
				</Style.Triggers>
			</Style>
		</Grid.Style>
	</Grid>
</UserControl>